# 운때 — 작업 규칙

## 커밋 · 푸시

작업이 끝나면 **묻지 말고 바로 `main`에 커밋·푸시**한다. 승인을 기다리지 않는다.

- 작업 브랜치를 따로 만들지 않는다. `main`에서 바로 작업하고 `git push origin main`.
  (사용자가 2026-09-18에 명시적으로 지시. 이 레포 한정.)
- 푸시하면 GitHub Pages가 재배포되어 웹에서 바로 확인할 수 있어야 한다.
  그러므로 **푸시 전에 동작을 검증한다** — 깨진 채로 올리면 사용자가 보는 페이지가 깨진다.
- 푸시 실패가 네트워크 오류일 때만 2s → 4s → 8s → 16s 로 최대 4회 재시도한다.
- 다음은 이 규칙에 포함되지 않으며 그때그때 확인받는다:
  force-push / 히스토리 재작성 / 브랜치·태그 삭제 / 파일 대량 삭제

## 다른 레포와의 관계

`welcome` 레포와는 **완전히 별개 프로젝트**다. 코드·설정·규칙을 서로 가져오거나 맞추지 않는다.

## 확정된 설계 결정 (다시 논의하지 말 것)

### 계산 기준 — 고정이며 옵션으로 되돌리지 않는다

- **정자시** (23시부터 다음 날 일주). 근거: 60 × 12 × 60 × **12** = 518,400 은
  하루가 12시진일 때만 성립한다. 야자시설은 하루를 13구간으로 쪼개 이 전제를 깬다.
- **진태양시** 상시 적용. 기준선은 **동경 127.5도 하나로 고정**, 보정량 정확히 −30분.
  근거: 동경 135도는 일본 아카시 자오선으로 한국 국토(124~132도) 밖이다.
  지역별 고유 경도와의 차이는 최대 6분(부산)이라 출생 지역을 묻지 않는다.
  보정은 시주뿐 아니라 **자시 경계(= 일주 경계)까지 함께 이동**시킨다.
  일주가 바뀌는 시점은 벽시계 23:00이 아니라 **23:30**이다.
  → 이 동작이 깨지면 회귀다. `saju-core.js` 수정 시 반드시 확인할 것.
- **입력은 10분 단위**. 30분 단위는 시주가 한 칸 밀릴 확률이 12.5%로 너무 높다(10분은 4.2%).
  입력란을 다시 분 단위로 되돌리거나 지역 선택을 되살리지 말 것.

### 화면에 쓰지 않는 것 (사용자 요청)

- 진태양시·정자시 기준 안내 문구 — 근거는 이 문서와 README에만 둔다
- "10분 단위" 같은 입력 규칙 설명 — `step=600` 으로 동작만 시킨다
- **경고 전부** (`절기경계` · `자시경계` · `시지경계` · `표준시특이`) — 엔진은 계속 계산해
  `saju.경고` 로 돌려주지만 화면에는 아무것도 띄우지 않는다. 되살리지 말 것.

### 용어 축 — 뒤집지 말 것

- 사주 = **운** (타고난 바탕) / 타로 = **때** (지금의 흐름)
- 화면과 문서 모두 `운 → 때 → 운때` 순서를 따른다.

### 질문 주제

`fortune-data.js` 의 `주제` 배열이 단일 출처. 주제를 고르면 추천 스프레드가 자동 선택되고,
`조립(saju, draw, sp, {갈래})` 로 총평의 갈래별 조언이 바뀐다.

**질문을 적으면 배열을 짚어 준다.** `배열추천()` 이 질문 모양을 보고 더 맞는 배열을 제안한다.
규칙은 **좁은 것부터** 본다 — '언제쯤 정리될까요' 가 '될까' 에 먼저 걸리면 시간 질문이
예·아니오로 잡힌다. 예·아니오는 의문사(왜·언제·어떻게·무엇…)가 있으면 제외한다.

**주제의 추천 배열이 그 질문에 실제로 답하는지 확인할 것.** '오늘·전체 흐름' 하나에
두 가지를 묶어 두었더니 "올해 흐름은?" 을 물었는데 "오늘 자리에 놓인 것은…" 으로
답하고 있었다. 주제는 '오늘 하루' 와 '전체 흐름' 으로 갈랐다.
예시 질문도 추천 배열이 답할 수 있는 모양으로 쓴다.

**질문 입력을 필수로 만들지 말 것.** 자유 입력을 강제하면 대부분 비우거나 못 읽을 문장을
넣는다. 한 번 누르는 주제 칩이 같은 일을 더 잘 하고, 스프레드 추천까지 얹을 수 있다.

**주제를 바꾸면 질문 입력란을 비운다.** 앞 주제에 맞춰 적은 질문이 남아 있으면
결과에 엉뚱한 질문이 함께 찍힌다.

입력 패널은 위에서 아래로 한 줄기로 읽혀야 한다 —
**주제 칩 → 예시 질문 → 질문 정하기(요령) → 질문 입력 → 스프레드 → 배열 설명·안내 → 뽑기**.
질문 요령을 질문 칸 뒤에 두거나, 스프레드를 질문보다 앞에 두지 말 것.

`갈래별칭` 으로 연애→관계, 전반→흐름 문장을 재사용한다. 새 주제를 더할 때는
전용 6문장을 쓰거나 별칭으로 기존 세트를 가리키게 한다.

### 예 · 아니오 배열

`판정: true` 인 스프레드는 총평 맨 앞에 **답부터** 낸다.
판정은 정/역(주 신호)과 카드 음양(보조 신호) 두 가지로만 낸다 — 4가지 경우.
둘이 같으면 단정(예/아니오), 엇갈리면 조건부.
그 뒤에 사주 강약으로 단서를 한 줄 붙인다.

**배열의 쓰임에 없는 기능을 적지 말 것.** 원 카드 설명에 '예·아니오처럼 짧은 답이
필요할 때도 좋아요' 라고 써 놓고 정작 답을 내지 않고 있었다. 사용자가 "밥먹을까?"를
물었는데 하루 기운 설명이 나왔다. 약속한 것은 구현하거나 문구에서 빼야 한다.

### 갈래 제목

화면 소제목은 `갈래라벨` 표에서 가져온다. `갈래 + '에 대해서는'` 으로 조립하면
'판정에 대해서는' 같은 말이 나온다.

### 스프레드

`fortune-data.js` 의 `스프레드` 배열이 단일 출처다. 항목 하나를 추가하면
선택 UI · 자리 미리보기 · 카드 배치 · 흐름 타임라인 · 총평이 전부 따라온다.
`위치[].축`(과거/현재/미래)은 십성해설을 재사용하기 위한 시제 분류이며 반드시 채운다.
UI에 스프레드 이름을 하드코딩하지 말 것.

### STEP 03 (운때 · 겹쳐 읽기)가 이 서비스의 핵심이다

사주와 타로를 나란히 늘어놓기만 하면 의미가 없다. 반드시 **둘이 만나는 지점**을 계산한다.
결론 카드의 오행이 사주 오행분포에서 과다/적정/부족 중 무엇인지가 그 지점이다
(평균 +1 이상 과다, -1 이하 부족). 판단 근거를 화면에 함께 적어 검산 가능하게 둔다.

**세 단의 축을 섞지 말 것.**
- `01 운` 은 사주만 말한다. 카드 이야기를 넣지 않는다.
- `02 때` 는 타로만 말한다. 일간·십성·사주 같은 말이 나오면 안 된다.
- `03 운때` 에서만 둘을 겹친다.
사용자가 직접 지적한 문제다 — 때 안에서 운을 끼워 설명하니 축이 흐려져 헷갈렸다.
바꾼 뒤에는 `02 때` 의 렌더 결과에 `일간|사주|비겁|식상|재성|관성|인성` 이
하나도 없는지 확인한다.

총평은 여섯 부분으로 조립한다 —
운 / 때 / 자리마다 겹쳐 보기 / 결론 자리 겹침 / 갈래별 조언 / 맺음.
어느 하나를 빼면 '종합'이 아니라 나열이 된다.

`자리마다 겹쳐 보기` 는 십성과 시제가 같으면 같은 문장이 나온다.
되풀이되는 줄은 '앞 자리와 같은 관계입니다' 로 접는다.

**인과가 보이게 쓴다.** 방향(정/역)만 말하고 끝내지 말 것.
'기운이 안쪽으로 접혀요' 처럼 추상으로 쓰면 왜 그런지 알 수 없다.
- 카드별 해설의 역방향 줄은 그 카드의 **바로 놓였을 때 뜻**을 들어 보인다 —
  "바로 놓였다면 「치우치지 않는 저울」이었을 자리가 뒤집혀 나왔습니다."
  바로 나온 카드는 덧붙일 말이 없으므로 줄을 아예 비운다.
- 맺음 꼬리는 '무엇의 카드인가 → 어느 방향 → 그래서 무엇' 순으로 적는다.

**조사를 문자열에 박아 넣지 말 것.** 카드 이름과 핵심어가 데이터라 받침이 제각각이다.
`받침()` · `은는()` 으로 판정한다. 실제로 78장 중 61장이 '있다이었을' 로 나오고 있었다.

맺음의 행동 문장은 **'타고난 쪽'** 으로 사주를 가리킨다. 예전에는 '사주로 보면 알맞게
갖춘 힘이에요' 처럼 썼는데, 무엇의 힘인지가 빠져 허공에 뜬 말이 됐다.
행동은 진단을 되풀이하지 말고 전제로만 쓴다 — 등급이 과다면 '타고난 쪽도 같은 방향이라',
적정이면 '타고난 쪽이 뒷받침해 주니', 부족이면 '타고난 쪽이 얇아' 로 이어 붙인다.

**행동 지시는 사주 강약(과다/적정/부족) 한 곳에서만 나온다.** 겹침은 진단만 하고,
맺음의 정/역 꼬리는 카드가 어떻게 놓였는지만 관찰한다. 이 규칙을 깨면
"압력이 모양을 잡아주는 배치" 다음에 "부담으로만 남습니다"가 붙는 식으로
한 화면에서 서로 반대되는 조언이 나온다. 실제로 1,000건 생성해 잡아낸 버그다.

`운` 문장은 일간 오행과 최강 오행이 다를 때 연결어를 넣는다. 사주 열에 여섯이
이 경우라, 연결어가 없으면 "금 일간인데 뻗어나가려는 기운이 강해요"로 읽힌다.

**총평 문장에 명리·타로 용어를 쓰지 않는다.** 상생/역극 같은 관계는 `관계쉬움` 표를 거쳐
쉬운 말로 바꾼다. 전문 용어는 타임라인 칩과 흐름 노트에만 남긴다.
십성 배지에는 `십성짧은뜻`을 붙인다 — '인성' 세 글자만 보고는 아무도 모른다.

진단 문장의 끝맺음을 한 가지로 통일하지 말 것. 여러 번 뽑으면 기계가 쓴 티가 난다.

양자택일에서 두 갈래의 진단이나 행동이 같게 떨어지면 **하나로 합친다**.
같은 문장을 두 번 읽히게 두지 말 것. 카드 방향만 다르고 처방이 같으면
꼬리는 갈래별로, 행동은 '두 쪽 모두'로 묶는다.

맺음 꼬리는 **카드 이름으로 시작한다** — 「완드 5」가 뒤집혀 나왔습니다.
십성 설명("나를 누르는 기운")으로 시작하면 바로 위 진단과 같은 말로 시작하게 된다.
실제 질문 1,000건 중 249건(25%)이 그랬다.

해석 문장을 고친 뒤에는 한 사주로 1,000건 생성해 아래 다섯이 모두 0인지 확인한다.
블록 간 어휘 반복(3글자 덩어리 겹침 22% 초과) / 때 문장의 전문 용어 /
미처리 조사 `은(는)` 류 / undefined·null / 한 총평 안의 동일 문장 반복.

숫자를 말할 때 '뽑기의 경우의 수'(천문학적)와 '사람이 쓴 문장 수'를 섞지 말 것.
문장 수는 `FortuneData.부품수` 가 실제로 세어 돌려준다 (하드코딩 금지).

### 화면 용어를 한쪽으로 통일한다

바깥에 보이는 말은 **'카드 배열'**, 괄호로 '스프레드라고 부릅니다' 를 한 번만 덧붙인다.
사용법·라벨·안내에서 '스프레드' 와 '배열' 을 섞어 쓰지 말 것.

`틀`(자리가 보는 것)은 **'자리'로 끝나지 않게** 쓴다. 화면에서
"이 자리가 보는 것은 「… 자리」입니다" 로 겹쳐 읽힌다. '곳' · '지점' · '것' 으로 끝낸다.

### 결과를 본 뒤의 흐름

- 첫 뽑기 뒤 사용법 패널(`#howto`)은 스스로 접힌다. 다시 펼 수 있게 `<details>` 로 둔다.
- 결과 맨 아래에 **다시 뽑기 버튼**을 둔다. 맨 위로 올라가게 만들지 말 것.

### 글씨 크기 바닥선

본문 12px(.75rem) 아래로 내리지 말 것. 마이크로 라벨(eyebrow·tag·자리 이름)은
11.2px(.7rem)까지만 허용하고, 역방향 배지 한 글자만 예외로 10.2px(.64rem)를 쓴다.
폼 컨트롤은 **반드시 16px(1rem)** — 그 아래면 iOS 사파리가 포커스할 때 페이지를
확대해 입력란이 화면 밖으로 밀려 나간다. 실제로 겪은 버그다.

### 반응형 검증

바꾼 뒤에는 1440 / 1024 / 768 / 560 / 430 / 414 / 390 / 375 / 360 / 320px 에서
9종 스프레드를 모두 뽑아 `scrollWidth - clientWidth` 가 0인지 확인한다.
자주 터지는 곳 둘 — 카드 자리 이름의 min-content 너비(그리드 자식에 `min-width:0`),
그리고 마디가 넷 이상인 흐름 타임라인(`.flowline.long` 으로 모바일에서 세로 전환).

### 자료실 (재료 살펴보기)

페이지 맨 아래 접이식 `<details id="ref">` 안의 탭 6종. 데이터는 전부 모듈에서 읽어
그린다 — 화면에 용어 표를 하드코딩하지 말 것. 카드·스프레드를 추가하면 자료실도 따라온다.

**햄버거 메뉴를 만들지 말 것.** 이동할 화면이 없는 단일 흐름이라 잘못된 신호를 준다.
새 용어가 생기면 자료실에 탭이나 항목을 더하고, 그 용어가 처음 나오는 자리에
`ref('<탭id>', '<라벨>')` 로 바로가기 버튼을 둔다.

탭 내용은 처음 열 때만 그리고 캐시한다. 78장 그리드가 가장 무겁다.

### 디자인 컨셉

**미드나잇 코스모스** — 딥 네이비·차콜 배경, 무광 골드 선화, 세리프 타이포.
명식 한자와 타로 카드를 같은 굵기의 금색 선으로 통일한다.
결과는 `01 운 → 02 때 → 03 운때` 세로 레일 + 과거·현재·미래 흐름 타임라인.

## 캐시 — JS를 고치면 버전을 반드시 올린다

GitHub Pages는 JS를 캐시한다. 버전을 안 올리면 사용자는 새 `index.html` +
**옛 JS** 조합을 받고, 조용히 `TypeError`로 깨진다 (실제로 한 번 겪었다).

JS 파일을 하나라도 고치면 **네 곳을 같은 값으로** 올린다.

1. `index.html` 의 `<script src="...?v=XXX">` 세 줄
2. `index.html` 의 `var 필요버전 = 'XXX'`
3. `saju-core.js` / `tarot-data.js` / `fortune-data.js` 각각의 `버전: 'XXX'`

버튼을 누르면 `버전확인()` 이 셋을 대조해 어긋나면 강력 새로고침을 안내한다.

## 코드 규칙

- 빌드 없음. 런타임 의존성 없음. `index.html`을 브라우저로 바로 연다.
- 폰트: 본문은 **Pretendard Variable 동적 서브셋**(jsDelivr) 한 줄만 외부에서 받는다.
  워드마크와 한자 명식만 시스템 명조(`--serif`)를 쓴다 — 웹폰트를 받지 않는다.
  CDN이 막혀도 시스템 산세리프로 떨어져 정상 동작해야 한다. 웹폰트를 더 늘리지 않는다.
- 실시간 AI 호출 금지. 해석 문장은 부품으로 준비하고 실행 시 조립한다.
- 식별자와 주석은 기존 코드대로 한국어를 쓴다.
- UMD 패턴(`module.exports` / `root.X`)을 유지한다 — node로 바로 테스트할 수 있어야 한다.

## 검증

브라우저 확인이 필요하면 Chromium이 이미 깔려 있다.

```
executablePath: '/opt/pw-browsers/chromium-1194/chrome-linux/chrome'   # --no-sandbox
```

로직만 볼 때는 node로 충분하다.

```
node -e "var S=require('./saju-core.js');console.log(S.calc({year:1990,month:5,day:15,hour:10,minute:30},{longitude:126.98}).팔자)"
```
