# 운때 — 작업 규칙

## 커밋 · 푸시

작업이 끝나면 **묻지 말고 바로 `main`에 커밋·푸시**한다. 승인을 기다리지 않는다.

- 작업 브랜치를 따로 만들지 않는다. `main`에서 바로 작업하고 `git push origin main`.
  (사용자가 2026-09-18에 명시적으로 지시. 이 레포 한정.)
- 푸시하면 GitHub Pages가 재배포되어 웹에서 바로 확인할 수 있어야 한다.
  그러므로 **푸시 전에 동작을 검증한다** — 깨진 채로 올리면 사용자가 보는 페이지가 깨진다.
- 푸시 실패가 네트워크 오류일 때만 2s → 4s → 8s → 16s 로 최대 4회 재시도한다.
- 다음은 이 규칙에 포함되지 않으며 그때그때 확인받는다:
  force-push / 히스토리 재작성 / 브랜치·태그 삭제 / 파일 대량 삭제

## 다른 레포와의 관계

`welcome` 레포와는 **완전히 별개 프로젝트**다. 코드·설정·규칙을 서로 가져오거나 맞추지 않는다.

## 확정된 설계 결정 (다시 논의하지 말 것)

### 계산 기준 — 고정이며 옵션으로 되돌리지 않는다

- **정자시** (23시부터 다음 날 일주). 근거: 60 × 12 × 60 × **12** = 518,400 은
  하루가 12시진일 때만 성립한다. 야자시설은 하루를 13구간으로 쪼개 이 전제를 깬다.
- **진태양시** 상시 적용 (출생지 경도 보정, 기본 서울 126.98).
  근거: 동경 135도는 일본 아카시 자오선으로 한국 국토(124~132도) 밖이다.
  보정은 시주뿐 아니라 **자시 경계(= 일주 경계)까지 함께 이동**시킨다.
  서울 기준 일주가 바뀌는 시점은 벽시계 23:00이 아니라 **23:33**이다.
  → 이 동작이 깨지면 회귀다. `saju-core.js` 수정 시 반드시 확인할 것.

### 용어 축 — 뒤집지 말 것

- 사주 = **운** (타고난 바탕) / 타로 = **때** (지금의 흐름)
- 화면과 문서 모두 `운 → 때 → 운때` 순서를 따른다.

### 디자인 컨셉

**미드나잇 코스모스** — 딥 네이비·차콜 배경, 무광 골드 선화, 세리프 타이포.
명식 한자와 타로 카드를 같은 굵기의 금색 선으로 통일한다.
결과는 `01 운 → 02 때 → 03 운때` 세로 레일 + 과거·현재·미래 흐름 타임라인.

## 코드 규칙

- 빌드 없음. 런타임 의존성 없음. `index.html`을 브라우저로 바로 연다.
  (Google Fonts 한 줄만 외부. 없어도 시스템 세리프로 정상 동작해야 한다.)
- 실시간 AI 호출 금지. 해석 문장은 부품으로 준비하고 실행 시 조립한다.
- 식별자와 주석은 기존 코드대로 한국어를 쓴다.
- UMD 패턴(`module.exports` / `root.X`)을 유지한다 — node로 바로 테스트할 수 있어야 한다.

## 검증

브라우저 확인이 필요하면 Chromium이 이미 깔려 있다.

```
executablePath: '/opt/pw-browsers/chromium-1194/chrome-linux/chrome'   # --no-sandbox
```

로직만 볼 때는 node로 충분하다.

```
node -e "var S=require('./saju-core.js');console.log(S.calc({year:1990,month:5,day:15,hour:10,minute:30},{longitude:126.98}).팔자)"
```
